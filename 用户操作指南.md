# EPUB 转 DOCX 转换器 - 用户操作指南

## 一、软件简介

EPUB 转 DOCX 转换器是一款简洁高效的电子书格式转换工具，帮助用户将 EPUB 格式的电子书转换为 Microsoft Word (DOCX) 格式，方便在各类文档编辑器中阅读和编辑。

## 二、快速开始

### 2.1 安装与运行

1. **安装 Python 3.8 或更高版本**
2. **安装依赖库**：
   - 双击运行 `run.bat`（Windows 用户）
   - 或在命令行执行：`pip install -r requirements.txt`
3. **启动程序**：
   - Windows：双击 `run.bat`
   - 所有系统：`python main.py`

### 2.2 基本操作步骤

```
┌─────────────────────────────────────────────┐
│  步骤 1：选择 EPUB 文件                      │
│  点击"浏览..."按钮，选择要转换的 .epub 文件     │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│  步骤 2：设置输出位置                         │
│  指定转换后 .docx 文件的保存路径               │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│  步骤 3：配置选项（可选）                      │
│  □ 包含目录  □ 保留图片                       │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│  步骤 4：开始转换                             │
│  点击"开始转换"按钮，等待转换完成              │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│  步骤 5：查看结果                             │
│  转换成功后，弹出成功提示，转换日志显示详情     │
└─────────────────────────────────────────────┘
```

## 三、界面功能说明

### 3.1 输入文件区域
- **文本框**：显示选中的 EPUB 文件完整路径
- **浏览按钮**：打开文件选择对话框，支持 `.epub` 格式

### 3.2 输出路径区域
- **文本框**：显示 DOCX 文件的保存路径
- **浏览按钮**：打开另存为对话框，可自定义文件名和保存位置

### 3.3 转换选项区域
- **包含目录**：勾选后，转换后的 DOCX 文档将包含原书的目录结构
- **保留图片**：勾选后，转换过程中会提取并保留 EPUB 中的图片

### 3.4 进度显示
- **进度条**：实时显示转换进度（0-100%）
- **状态标签**：显示当前转换状态（如"正在转换章节 3/10..."）

### 3.5 转换日志
- **日志区域**：滚动显示转换过程的详细信息
- **清空按钮**：清除当前日志内容

## 四、高级功能

### 4.1 批量转换
如需批量转换多个文件，可以编写脚本调用转换模块：

```python
from epub_converter import EPUBConverter

files = ['book1.epub', 'book2.epub', 'book3.epub']
for epub_file in files:
    output = epub_file.replace('.epub', '.docx')
    converter = EPUBConverter(epub_file, output)
    result = converter.convert()
    print(f"转换结果: {result['success']}")
```

## 五、故障排除

### 5.1 常见问题

| 问题 | 解决方案 |
|------|----------|
| 程序无法启动 | 检查 Python 版本是否为 3.8+ |
| 依赖安装失败 | 使用国内镜像源：`pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt` |
| 转换失败 | 确认 EPUB 文件未损坏且格式标准 |
| 图片无法显示 | 确认原 EPUB 文件包含图片，并勾选"保留图片"选项 |
| 进度卡住 | 部分 EPUB 格式复杂导致处理时间长，请耐心等待 |

### 5.2 错误信息解读

- **"文件不存在"**：检查文件路径是否正确
- **"权限不足"**：以管理员身份运行或更换输出目录
- **"格式不支持"**：当前工具仅支持标准 EPUB 格式

## 六、技术支持

如遇到其他问题，请提供以下信息：
1. 操作系统类型和版本
2. Python 版本
3. 完整的错误信息截图
4. EPUB 文件名（不包含实际文件）

## 七、注意事项

1. **备份原文件**：转换前建议备份原始 EPUB 文件
2. **格式差异**：EPUB 与 DOCX 格式特性不同，转换后格式可能有细微差异
3. **版权提醒**：请仅转换您拥有版权的电子书，尊重知识产权
4. **大文件处理**：大型 EPUB 文件转换可能需要较长时间，请耐心等待
